(function ($) {

    var methods = {

    };
    $.fn.HCApi = function () {
        // there's no need to do $(this) because
        // "this" is already a jquery object
        // $(this) would be the same as $($('#element'));


    };
})(jQuery);